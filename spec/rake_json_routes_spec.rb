require 'spec_helper'

describe RakeJsonRoutes do
  it 'has a version number' do
    expect(RakeJsonRoutes::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
